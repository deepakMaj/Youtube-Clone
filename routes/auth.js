const express = require('express');
const router = express.Router();

// @route GET /api/auth
router.get('/', (req, res) => {
  res.send()
});

// @route POST /api/auth
router.get('/', (req, res) => {
  res.send()
});

module.exports = router;